import { Pool } from "commons";

export default async (pool: Pool) : Promise<void> => {
    //buscar as automações

    //testar as condições delas

    //fazer o swap

    //atualizar a automação

    //registrar o trade
}